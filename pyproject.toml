[project]
name = "acl-fasteat-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "dotenv>=0.9.9",
    "langchain>=1.4.0",
    "langchain-openai>=1.6.2",
    "tavily>=1.1.0",
]

[project.scripts]
acl-fasteat-agent = "acl_fasteat_agent:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
